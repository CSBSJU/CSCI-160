// FileNotFoundException, PrintWriter
import java.io.*;

// Scanner
import java.util.Scanner;

/**
 * Utilities for double arrays
 * 
 * @author <Your name>
 */
public class DoubleUtilities
{
  /**
   * Selects a file, reads the file and returns a double array of data from the
   * file.
   *
   * @param filePath the path to the file to read the values from
   *
   * @return a double array containing the data read from the file
   */
  public static double [] readFile(String filePath)
  {
    //*** REMOVE AFTER COMPLETING THIS METHOD
    return null;
  }

  /**
   * Writes the specified double array into a selected file.
   *
   * @param filePath    the path to the file to write the values in
   * @param doubleArray the array of doubles to write
   */
  public static void writeFile(String filePath, double [] doubleArray)
  {
  }

  /**
   * Generates numDoubles random double values and returns an array containing
   * those values.
   *
   * @param  numDoubles the number of values to generate
   * @param  low        the lower bound of values to generate
   * @param  high       the upper bound of values to generate
   *
   * @return an array containing numDoubles random double values
   */
  public static double [] generateValues(int numDoubles, double low, double high)
  {
    //*** REMOVE AFTER COMPLETING THIS METHOD
    return null;
  }

  /**
   * Sorts an array of double values using the insertionSort algorithm.
   *
   * @param doubleArray the array of doubles to sort
   */
  public static void sort(double [] doubleArray)
  {
  }
}
